[pytest]
addopts = --alluredir=allure-results --clean-alluredir -v --tb=short --html=reports/report.html --self-contained-html --css=reports/custom_report.css -m "not pending"
testpaths = tests
console_output_style = count
markers =
    pending: módulo aguardando captura de locators via Appium Inspector (excluído da execução padrão, ver README)
